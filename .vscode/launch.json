{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Django Debug Test",
      "type": "debugpy",
      "request": "launch",
      "justMyCode": false,
      "program": "${workspaceFolder}/manage.py",
      "args": [
        "test",
        // "`echo -n \"${relativeFileDirname}\" | tr \\\\ .`.${fileBasenameNoExtension}"
      ],
      "django": true
    }
  ]
}
